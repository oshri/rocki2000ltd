import React from 'react';
import Link from 'next/link';

import './Splash.scss';

const Splash = (props) => {
    const { size } = props;

    return (
        <div className="Splash">
            
        </div>
    );
};


export default Splash;