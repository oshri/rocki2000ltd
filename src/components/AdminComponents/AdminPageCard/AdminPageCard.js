const AdminPageCard = ({name, link, id, template, icon, children}) => {

    return (
        <div className="AdminPageCard">
            <h3>{name}</h3>
        </div>
    );
};


export default AdminPageCard;